public struct AlterableStats
{
    public float currentHP;
    public float currentMana;

    public AlterableStats(float currentHP, float currentMana)
    {
        this.currentHP = currentHP;
        this.currentMana = currentMana;
    }
}
